apiVersion: v2
name: faros-kuery-provider
description: |
  Faros provider for fleet-wide object search, relationship traversal, and
  impact analysis across connected edge clusters (built on faroshq/kuery).
  Ships the provider Deployment, ClusterIP Service, and the CatalogEntry
  that registers the provider (UI + backend + the SavedView APIExport +
  edgeProxyAccess) with the faros hub. Phase 1 skeleton.
type: application
version: 0.1.0
appVersion: "0.1.0"
icon: https://example.com/faros-icon.png
keywords:
  - faros
  - provider
  - kuery
  - observability
home: https://github.com/faroshq/faros
sources:
  - https://github.com/faroshq/faros/tree/main/providers/kuery
maintainers:
  - name: faros maintainers
